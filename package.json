{
  "name": "karta",
  "version": "0.1.0",
  "description": "Local MCP server that gives Claude Code stack-aware reconnaissance tools (NestJS / Next.js / Node TS).",
  "type": "module",
  "bin": {
    "karta": "./dist/index.js",
    "karta-viewer": "./dist/viewer/cli.js"
  },
  "main": "./dist/index.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "viewer": "node dist/viewer/cli.js",
    "dev": "tsc -w",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.4",
    "ts-morph": "^24.0.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "@vitest/coverage-v8": "^4.1.7",
    "typescript": "^5.5.4",
    "vitest": "^4.1.7"
  }
}
